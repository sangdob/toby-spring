
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/toby_spring?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: qwer1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    show-sql: true
    generate-ddl: false
    hibernate:
      naming:
        implicit-strategy: org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy
      ddl-auto: create
      dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: true