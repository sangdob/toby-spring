
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/toby_spring
    username: root
    password: qwer1234

  jpa:
    database: mysql
    show-sql: true
    generate-ddl: false
    hibernate:
      naming:
        implicit-strategy: org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy
      ddl-auto: create
      dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: true